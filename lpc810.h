#ifndef __LPC810_H
#define __LPC810_H

#include "dual.h"

/* hold this idea in reserve
// this is a buffer which can contain one of many types of array, to save RAM
union cpoles_or_panels {
    struct cpole cpoles[MAX_WALLS];
    struct panel panels[MAX_WALLS];
};

// this is a buffer which can contain one of many types of array, to save RAM
union spoles_or_rles {
    struct spole spoles[MAX_WALLS];
    struct rle rles[MAX_COLOUR_CHANGES];
};
*/

#endif